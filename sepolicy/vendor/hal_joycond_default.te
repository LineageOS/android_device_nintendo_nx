# Access to JoyCons
r_dir_file(hal_joycond_default, sysfs_hid)
allow hal_joycond_default sysfs_hid:file rw_file_perms;

r_dir_file(hal_joycond_default, sysfs_serial)
allow hal_joycond_default sysfs_serial:file rw_file_perms;

# TMP - Access to JoyCons - before LMK labels it
r_dir_file(hal_joycond_default, sysfs)

set_prop(hal_joycond_default, vendor_persist_joycond_prop)
