allow init servicemanager:binder { call transfer };
allow init system_server:binder { call transfer };

# neverallow wifi_reset.sh
allow init settings_service:service_manager find;
allow init wifi_reset_exec:file {execute execute_no_trans r_file_perms};
allow init system_file:file {execute execute_no_trans};

init_daemon_domain(wifi_reset_exec)
