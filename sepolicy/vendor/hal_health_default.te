# Access to JoyCons
r_dir_file(hal_health_default, sysfs_serial)

# Access to Sensors
r_dir_file(hal_health_default, sysfs_iio)

# Access to USB firmware
r_dir_file(hal_health_default, vendor_firmware_usb_file)
